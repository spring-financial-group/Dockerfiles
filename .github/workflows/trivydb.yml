name: Trivy offline DB

on:
  schedule:
    - cron:  '0 7 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout v2
      uses: actions/checkout@v2

    - name: Login to ghcr.io
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ secrets.USER }}
        password: ${{ secrets.TOKEN }}

    - name: Build the Docker image
      run: |
        docker build trivydb -t ghcr.io/spring-financial-group/trivydb:latest
        docker push ghcr.io/spring-financial-group/trivydb:latest
    env:
      GITHUB_TOKEN: ${{ secrets.TOKEN }}
      USER: ${{ secrets.USER }}